/*
// 100 created, 101 proccess, 102 finish
enum STATUS { created = 100, process = 900, finish = 500};

let todoStatus : STATUS = STATUS.finish;

console.log(todoStatus);
*/

// Ví dụ đầy đủ.
/*
enum Status {
    Active = "ACTIVE",
    Inactive = "INACTIVE",
    Pending = "PENDING"
}

function printStatus(status: Status) {
    if (status === Status.Active) {
        console.log("The status is active.");
    }
}

printStatus(Status.Active);  // Output: "The status is active."

*/